# 變更紀錄

herdrm 所有值得記錄的變更都寫在這個檔案裡。格式參考
[Keep a Changelog](https://keepachangelog.com)；版本號遵循 semver。
發佈自動化流程會擷取對應版本的段落，作為 GitHub release 說明與 Sparkle 的更新描述 —
沒有對應段落的版本會讓 CI 失敗。

## [Unreleased]

### 新增
- 選單列常駐圖示：列出所有裝置上被卡住或已完成的代理人，點一下就跳到它；
  Dock 圖示會顯示待回應的數量。可在「外觀」設定關閉。
- blocked 通知現在直接顯示代理人問的那句話，而且可以在通知裡輸入文字回覆，
  不必切回 App。
- 鍵盤導覽：⌥⌘↑／⌥⌘↓ 切換代理人，⌘1–9 直接跳到第 n 個，⌘N／⇧⌘N／⌘K
  進入主選單。方向鍵本身仍留給終端機裡的 TUI。
- 側邊欄新增 Terminals 分組，列出 herdr 回報但沒有代理人的 pane，選取後
  一樣可以直接操作。
- 滑過側邊欄的列會顯示該 pane 最後幾行輸出的 tooltip。
- 重新啟動後會回到上次開著的代理人。
- 終端機 attach 中斷時顯示「已中斷連線」與重新連線按鈕，不再只是靜靜凍住。

### 變更
- 切換代理人不再重建 attach 程序：最近看過的終端機會保活（上限 2 個、
  逾時 45 秒），且所有 ssh 共用 ControlMaster 連線，遠端切換幾乎沒有延遲。
  刻意設上限與逾時，因為 herdr 會把仍附掛的 pane 當成你正在看而壓下
  它的完成通知。
- 已安裝的 agent CLI 改為每次連線成功時重新偵測，在遠端裝新的 CLI 之後
  不必重開 App 才看得到。
- ⌘K 搜尋改為大小寫與變音符號不敏感，並可搜尋工作目錄。

### 修正
- 通知授權狀態改為每次發送前查詢。過去只在啟動時記錄一次，使用者事後才在
  系統設定開啟通知的話，要重開 App 才會生效。
- 事件流仍存活時的單次 snapshot 失敗不再把裝置標成離線 —— 過去會停在沒有
  任何人重試的失敗狀態。
- SSH 通道的本機 socket 檔名改用穩定摘要，不再使用每次啟動都不同的
  `hashValue`（會留下孤兒 socket 檔，且兩台裝置有機率相撞）。
- 結束 App 時會收掉所有 `herdr agent attach` 子程序。

## [0.2.3] - 2026-08-19

### 新增
- 現在可以從側邊欄的右鍵選單重新命名 space。（#6，感謝 @hhmy27！）

## [0.2.2] - 2026-08-19

### 新增
- 終端機設定新增「Mouse reporting」開關 — 關掉之後，即使在會攔截滑鼠的 TUI 裡也
  一律能用滑鼠選取文字（不論開關狀態，Shift + 拖曳都可以選取）。（#2、#5）

### 修正
- App 主題切換時，終端機現在會立即重新繪製。（#4）

## [0.2.1] - 2026-08-19

### 修正
- 新增裝置的作業系統偵測改為每次連線成功時都重試，而不是只在加入裝置當下試一次；
  另外 SSH 會自動接受未知的主機金鑰（`accept-new`），所以新裝置第一次連線不會再因為
  你還沒手動 ssh 過去而失敗。

## [0.2.0] - 2026-08-19

### 新增
- 所有裝置現在都會同時保持連線：側邊欄會匯總每一台機器上的 space 與代理人，
  並用一個小的作業系統徽章標示每一列所屬的機器。左下角的切換器改成了過濾器
  （預設為 All Devices）。
- 通知現在會監看每一個已連線的裝置，而不只是目前選取的那一台；點擊通知會直接跳到
  該代理人。
- New Agent 與 New Space 加上了裝置選擇器；已安裝代理人的偵測結果會依裝置快取。
- 每個裝置各自的連線健康狀態與自動重連（1 秒 → 30 秒的退避重試）。

### 變更
- 搜尋結果會顯示裝置徽章，且搜尋範圍涵蓋所有裝置。

## [0.1.2] - 2026-08-19

### 新增
- New Space 現在有目錄選擇器（本機可用「Browse…」，遠端裝置支援 `~` 展開）以及一個
  選填的名稱欄位。

## [0.1.1] - 2026-08-19

### 修正
- 修正在 macOS 26 以上的 beta 版開啟裝置切換器時會當掉的問題：改用視窗內的面板
  取代 NSPopover（ViewBridge 中未被攔截的 NSRemoteView 例外）。

## [0.1.0] - 2026-08-19

### 新增
- 首次發佈：waku 風格的側邊欄（含 space 與代理人）、可加入 SSH 遠端主機的裝置切換器、
  SwiftTerm PTY attach、代理人偵測與略過權限旗標、⌘K 搜尋、Sparkle 自動更新，
  以及已簽章並公證的發佈版本。
